import React from 'react';
import SearchBar from '../SearchBar/SearchBar';

const NavBar = () => {
    return (
        <>
        NavBar
        <SearchBar />
        </>
    );
}
 
export default NavBar;